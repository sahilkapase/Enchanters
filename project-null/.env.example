# ── Database ──
DB_PASSWORD=change_me_in_production

# ── Auth ──
JWT_SECRET=super-secret-jwt-key-change-me

# ── OTP Provider ──
# console = print OTP to backend logs (dev mode)
# twilio  = send real SMS via Twilio
OTP_PROVIDER=console

# ── Twilio (only if OTP_PROVIDER=twilio) ──
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
